{
  "name": "hitd-mongo",
  "version": "1.0.0",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "start": "INPORT_CLIENT_ADDR=$(ip addr show eth0 | awk '/inet / { print $2 }' | cut -d '/' -f 1) node lib/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "async": "^0.9.0",
    "debug": "^2.2.0",
    "mongodb": "^2.0.39",
    "hitd-handler": "^1.1.7"
  }
}
